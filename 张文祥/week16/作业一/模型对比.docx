一、各模型结构演进详解（对比前置版本）
1.DeepSeek V4 Pro (0813)——前置版本 V3.2 / V3
前置基线：DeepSeek-V3.2（MoE，671B 总 / 37B 激活，128K 上下文，MLA 注意力）。
规模：总参数 671B → 1.6T，激活 37B → 49B；层数 61、hidden 7,168。
注意力重构（最核心改变）：V3 系列使用 MLA（Multi-head Latent Attention），V4 改为 CSA（压缩稀疏）+ HCA（重度压缩）混合注意力 + 128 token 本地分支，1M 上下文下单 Token FLOPs 仅为 V3.2 的 27%、KV Cache 为 10%。
新增 mHC（Manifold-Constrained Hyper-Connections）：替代常规残差连接，残差流 4× 扩展，改善深层信号传播稳定性。
新增训练基建：Muon 优化器 + FP4（专家）/FP8（其余）混合精度，替代 V3 的 BF16 为主方案。
后训练范式改变：由统一 SFT/RL，改为两阶段——先对各领域专家独立做 SFT+GRPO 强化，再 on-policy 蒸馏整合进单一模型。
新增推理模式：Non-think / Think High / Think Max 三档（V3 系列无显式档位）。
新增长上下文输出能力：最大输出 384K（V3 系列未支持）。
2.2 dots3-note Preview（小红书）——前置版本 dots.llm1
前置基线：dots.llm1（MoE，142B 总 / 14B 激活，纯文本，借鉴 DeepSeek 架构，WSD 学习率调度）。
规模：142B/14B → 280B/16B；层结构 1 稠密 + 45 MoE，hidden 5,120。
新增多模态：前置为纯文本；新增 MoE ViT 视觉编码器（7B 总/1.2B 激活）+ 稠密音频编码器（800M），支持图像/视频/音频输入。
注意力改变：前置沿用 DeepSeek 风格注意力；新增 13×DSA（Top-2048）+ 33×SWA（滑动窗口）混合（约 1:3），支撑 512K 上下文。
新增 MTP（Multi-Token Prediction）：1 个共享层 1.13B，多 Token 预测加速解码。
新增训练方法 TEMPO：Test-time-scaled Value Estimation with Macro-step Policy Optimization——前置为传统 RL；TEMPO 将超长轨迹切为 macro-step，actor/critic 切换并用 test-time scaling 估价值，解决长程稀疏奖励问题。
新增 Self-Critiquing 与主动记忆（memory.md）：支持开放环境中探索、纠错与长程任务持续交付。
2.3 Qwen3.8-Max (2.4T-A95B)——前置版本 Qwen3.5 系列
前置基线：Qwen3.5 系列（MoE 架构、传统 GQA 注意力、开源与闭源分线）。
规模：扩至 2.4T 总 / 95B 激活；92 层、hidden 8,192、词表 248,320。
新增线性注意力 Gated DeltaNet：与门控注意力按 3:1 混合（每 4 层：3×Gated DeltaNet→MoE + 1×Gated Attention→MoE），大幅降低长上下文 KV 开销——这是 Qwen 系列首次引入线性注意力架构。
MoE 改变：专家增至 512（每 Token 激活 10 路由 + 1 共享），专家中间维度 2,048。
新增多步 MTP（Multi-Token Prediction）：前置版本 MTP 为单步或未启用。
新增原生视觉：统一视觉-语言基础（早期融合训练于万亿级多模态 tokens），非外挂 OCR/描述，为 Qwen 开源线首次。
上下文：原生 262K、可扩至 1,010K，支持 201 种语言。
新增 Max 级开源：此前开源与 Max 商业线分离，本次首次开放 Max 权重。
2.4 Qwen3.8-27B——前置版本 Qwen3.5-27B 等稠密型号
前置基线：Qwen3.5 系列的 27B 稠密模型（文本为主）。
规模与架构：保持 27B 稠密（Dense）路线，不引入 MoE。
新增多模态：从文本模型升级为多模态理解/推理。
上下文：约 262K。
定位改变：作为 Qwen3.8-Max 的下沉配套开源版本，社区量化支持密集（GGUF/AWQ 等），适合本地部署。
2.5 GLM-5.3（智谱 Z.ai）——前置版本 GLM-5.2
前置基线：GLM-5.2（MoE，743B 总 / 约 40B 激活，256 专家 Top-8，MLA+DSA，1M 上下文，MIT）。
结构特点零改变：基础模型与 GLM-5.2 完全一致（同一 743B 权重），未重做预训练、未改架构。
改变集中在后训练：新增三大技术支柱——
IndexShare：长上下文训练/推理效率优化，支持 1000+ 连续训练步（约 6× 提升）。
SAO（Sequential Action Optimization）：单轨迹级异步训练，解耦训练与推理，稳定支撑 1000+ 步长轨迹（传统方法约 160 步即崩溃）。
Slime：开源的异步 RL 分布式后训练框架（THUDM/slime），Megatron+SGLang 统一数据流。
能力改变（由后训练带来）：自评编码能力较 GLM-5.2 提升约 50%；Terminal-Bench 3.0 开源模型第一；涌现网络安全能力——发现 2,436 个真实漏洞（最早可追溯至 1981 年）
